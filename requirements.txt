# Same stack as ./kyc/requirements.txt (Python 3.9, InsightFace, PaddleOCR, MediaPipe).
# Divergences from kyc are the ones that make a multi-arch image installable:
#   - no torch: nothing here imports it, and there is no torch==*+cpu wheel for aarch64
#   - PyYAML==6.0.2: paddlex (pulled in by paddleocr 3.4.x) pins exactly that
#   - paddlepaddle/paddleocr pinned: kyc used floating ranges; 3.2.2 / 3.4.1 are
#     the newest with cp39 wheels for both manylinux x86_64 and aarch64

fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0
Pillow==10.1.0
numpy==1.26.2
opencv-python-headless==4.8.1.78

insightface==0.7.3
onnxruntime==1.12.1
paddlepaddle==3.2.2
paddleocr==3.4.1

PyYAML==6.0.2
python-dotenv==1.0.0
aiofiles==23.2.1

protobuf>=3.20.0,<4.0.0
mediapipe==0.10.9
imutils==0.5.4
